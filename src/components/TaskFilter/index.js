import React, {Component} from 'react';
import PropTypes from 'prop-types';
import './style.scss';

const TaskFilter = (props) => {
    return (
        <div className="task-filter-handle">Task's Filter</div>
    )
}

TaskFilter.propTypes = {

}

export default  TaskFilter;